# Simulator vs Real-Client — Gap Classification

_Date_: 2026-04-21  
_Source_: `results/sim-vs-real-gap-report.md` (auto-generated by `scripts/diff_real_vs_sim.py`)  
_Real snapshot used_: `observer-20260420T223706/snapshots/tick-000010.json` (Ante 2 / Round 3 / SHOP, seed `4WAX5M4D`, RED deck, WHITE stake)

## Headline numbers

| Category | Count | % |
|---|---:|---:|
| aligned | 28 | 36.8% |
| value_mismatch | 26 | 34.2% |
| missing_in_sim | 22 | 28.9% |
| missing_in_real | 0 | 0.0% |
| shape_mismatch | 0 | 0.0% |
| **total** | **76** | |

## Classification of the 48 non-aligned fields

### A. Expected game-progress diff (not a real gap) — **~12 rows**
Sim is fresh (Ante 1 / Round 1 / PreBlind), real is mid-game (Ante 2 / Round 3 / SHOP). These diffs will disappear once we have a seed-aligned fresh real trajectory:
- `state`, `ante_num`, `round_num`, `money`
- `round.hands_played`, `round.discards_used`, `round.reroll_cost`
- `blinds.small.score`, `blinds.big.score`, `blinds.boss.score` (ante-scaled)
- `blinds.boss.name`, `blinds.boss.effect` (boss only named/assigned after ante 1 clear)
- `blinds.small.status`: UPCOMING vs SELECT (because ante 2 has all blinds UPCOMING before entry)

### B. Missing engine features — **real modeling gaps** — **24 rows** ⚠️
These are the **actual simulator modeling gaps** that need work.

#### B1. Skip-Blind Tag System — **DONE (Layer A) + partial (Layer B)** ✅
_Update 2026-04-20_: tags are now modeled end-to-end.

**Layer A — metadata visibility — shipped**
- `balatro-spec::TagSpec` added, `RulesetBundle.tags` populated (24 vanilla
  entries migrated from `vendor/balatro/steam-local/extracted/game.lua` +
  zh_CN localization).
- Engine rolls a tag per round for Small and Big slots (`tag.small.pick` /
  `tag.big.pick` RNG domains); Boss slot is always `None` per vanilla.
- `Snapshot.small_tag` / `big_tag` / `boss_tag` (`Option<TagInfo>`) exposed.
- `PySnapshot` getters added; `env/state_mapping.to_real_shape` populates
  `blinds.{small,big,boss}.tag_name` + `tag_effect` using the description
  string from the catalog.

**Layer B — effect semantics — 6 of 24 implemented, rest stubbed**

| Status | Tag | Effect |
|--------|-----|--------|
| ✅ | Economy Tag | `money += min(money, 40)` |
| ✅ | Investment Tag | Queues `$25` payout, paid in `handle_post_blind` after boss clear |
| ✅ | Voucher Tag | `refresh_shop` guarantees a voucher slot and sets its cost to `$0` |
| ✅ | Coupon Tag | `refresh_shop` zeros `buy_cost` on initial jokers, consumables, and packs |
| ✅ | D6 Tag | Next shop entry resets `shop_current_reroll_cost` to `$0` |
| ✅ | Juggle Tag | `enter_current_blind` applies `+3` to `hand_size` for next blind only (reverted in `handle_post_blind`) |
| ✅ | Speed Tag | `$5 * prior_skip_count` paid immediately on trigger |
| ✅ | Handy Tag | `$1 * total_hands_played` (sum across `hand_stats.played`) paid immediately |
| 🔲 stub | Negative / Foil / Holographic / Polychrome / Rare / Uncommon Tags | Requires shop-joker modification hook (next base joker becomes …) |
| 🔲 stub | Standard / Charm / Meteor / Buffoon / Ethereal Tags | Requires free booster-pack spawn infrastructure |
| 🔲 stub | Garbage Tag | Requires global unused-discard tally across the run |
| 🔲 stub | Boss / Double / Top-up / Orbital Tags | Niche mechanics |

All stubbed tags emit `unimplemented_tag` events on skip — no silent drops.

**Result on the diff report**:
- `blinds.{small,big,boss}.tag_name` and `blinds.{small,big,boss}.tag_effect`
  are now **present** in sim (no longer missing_in_sim).
- Boss rows are `aligned` (both empty strings).
- Small/Big rows are `value_mismatch` (different tag rolled because the sim
  seed differs from the recorded run's Lua seed) — expected.

Aligned/missing diff: **28→94 aligned, 22→16 missing_in_sim**.

#### B2. Per-Poker-Hand Statistics — **11 rows**
Real: `hands.*` is a full dict keyed by hand name ("Pair", "Flush", …) with `{level, played, played_this_round, order, chips, mult, example}` per entry. Used for both UI (hand list on right) and scoring (level → chips/mult).
Sim: has `hand_levels: {name: level}` (bare map). Missing `played`, `played_this_round`, `order`, `chips`, `mult`, `example`.

Paths: `hands.High Card` … `hands.Flush Five` (11 hand types including the 3 Steamodded-unlocked types: Flush House, Five of a Kind, Flush Five).

Engine work: extend `HandLevel` struct to carry play counters + base scoring + example cards. Reset `played_this_round` at round boot.

#### B3. Deck Composition Metadata — **2 rows**
Real: `cards.limit = 52`, `cards.highlighted_limit = 5`. Real client always exposes the full current deck composition (52 cards by default, more with Abandoned Deck etc.) alongside limits.
Sim: `cards.limit = None`, `cards.highlighted_limit = None`.

Engine work: add `deck_limit` and `play_limit` (i.e., max cards per play = 5) to snapshot.

#### B4. Pack Limits — **2 rows**
Real: `packs.limit = 2`, `packs.highlighted_limit = 1`.
Sim: `None` / `None`.

Engine work: trivial metadata add.

#### B5. `cards.cards` always populated — **2 rows (value_mismatch)**
Real: `cards.cards` is a list of 52 card dicts (full deck composition) regardless of phase.
Sim: `deck` field is empty (`list[0]`) in Stage_PreBlind; only populated during active round.

Engine work: materialize remaining-deck as `cards.cards` at all times, not just during round.

#### B6. Default Reroll Cost — **1 row (value_mismatch)**
Real: `round.reroll_cost = 5` in shop.
Sim: `shop_reroll_cost = 0` in fresh state (initialized only on shop entry).

Engine work: initialize reroll_cost to 5, not 0.

### C. Deck-modifier application — **~4 rows**
Real: RED deck → starts each round with **4 discards** (3 base + 1 from RED).
Sim fresh snapshot: `discards=3` because no deck selection is being respected (or default deck gives 3). Same for `plays` and `money` starting values.

Paths seeing this: `round.discards_left` (real 4 vs sim 3), plus transitively the per-round resets.

Engine work: plumb `deck` choice into Engine constructor; apply each deck's modifiers (RED: +1 discard, YELLOW: +$10 start, etc.).

### D. Action items — priority order

1. **P0 — plumb `deck` + string `seed` into Engine** (enables value-level diffing)
2. **P1 — extend `HandLevel` to full per-hand stats** (blocks `hands.*` alignment, and is the biggest user-visible gap)
3. **P1 — add skip-tag system** (6 rows gone; unlocks skip-blind training signal)
4. **P2 — materialize `cards.cards` at all phases + add metadata limits**
5. **P2 — reroll cost default**
6. **P3 (out of scope for 1.0)** — Steamodded extensions (`SMODS_BOOSTER_OPENED`, `PLAY_TAROT` states, Flush Five / Flush House types beyond Vanilla)

### E. Still-needed real-client data — for **semantic** (not just schema) verification

Current limitation: our real trajectory has seed `4WAX5M4D` (Lua string), our engine takes u64. We can match **shape** but not **values**. To do step-by-step semantic diffs:

1. **Fresh-seeded real session**: start a real run with a specific numeric-compatible seed (or record the Lua seed → u64 mapping).
2. **Ante-1-to-win trajectory**: want the full action sequence from `start` to `game_over`, not a mid-game snapshot.
3. **Coverage dataset**: 5–10 sessions per P1 plan.

## Tooling in place

| Path | Purpose |
|---|---|
| `env/state_mapping.py` | Normalizer: sim snapshot dict → real-client shape; plus diff primitives |
| `scripts/diff_real_vs_sim.py` | Produces the markdown report above |
| `results/sim-vs-real-gap-report.md` | Auto-generated raw field-by-field diff |

## Quick commands

```bash
# regenerate gap report any time
python scripts/diff_real_vs_sim.py \
    --real results/real-client-trajectories/observer-20260420T223706/snapshots/tick-000010.json \
    --sim-seed 42 --sim-stake 1 \
    --report results/sim-vs-real-gap-report.md
```
