# hack-balatro 当前进度（2026-03-09 15:50:30）

## 1. 基建建设进度

### 已完成
- 项目骨架已建立：`env/`、`agents/`、`training/`、`eval/`、`scripts/`、`configs/`、`tests/`、`utils/`、`models/`。
- 复现流程主 CLI 已落地：`scripts/repro.py`（含 `phase1/phase2/eval/report/resume` 子命令）。
- 配置中心已建立：`configs/repro.yaml`，支持多策略：
  - `strategy_stable`
  - `strategy_reward_boost`
  - `strategy_transformer`
- 评估协议与结果输出格式已建立：JSON + CSV（含 `win_rate`、`avg_blinds_passed`、`avg_episode_reward` 等）。
- 测试骨架已建立：obs 编码、fallback、Greedy 偏移量回归、rollout smoke。
- 上游来源已锁定：`third_party.lock`。
- 远端仓库已完成推送：`main` 分支在线。

### 本地上游代码状态
- 本地 `vendor/` 已有上游仓库克隆：
  - `vendor/balatro-rs`
  - `vendor/balatro-gym`
- 当前 `.gitignore` 已配置忽略 `vendor/`，所以这些内容是本地依赖，不会随提交上传。

## 2. 环境模拟进度（Environment Simulation）

### 已实现
- `BalatroEnv` 已实现（`env/balatro_gym_wrapper.py`）。
- obs/action 协议已实现并固定：
  - Observation: `454` 维
  - Action: `86` 维
- `StateEncoder` 已实现（`env/state_encoder.py`）：
  - 偏移量常量化导出
  - pack/unpack 结构化输入
  - hand/joker/boss 等分段编码
- Action mask 逻辑与非法动作 fallback 已实现。
- 提供了 mock engine fallback：在 `pylatro` 不可用时仍可跑通流程 smoke。
- `scripts/doctor.py` 可执行基础健康检查（维度、阶段流转、seeds 文件存在性）。

### 当前状态判断
- 模拟层代码已具备“可接入训练”的基础能力。
- 真实 Rust 引擎绑定（`pylatro`）尚未在当前机器完成安装验证，因此现在默认依赖 mock backend。

## 3. 训练 Readiness（部署与依赖）

### 已就绪
- Python 解释器：`Python 3.13.9`
- Rust 工具链：
  - `rustc 1.93.0`
  - `cargo 1.93.0`
- GitHub CLI：`gh 2.85.0`
- 基础 Python 包：`numpy`、`yaml(PyYAML)` 可导入。

### 未就绪 / 缺失
- `torch`：未安装
- `gymnasium`：未安装
- `pytest`：未安装
- `pylatro`：未安装（未完成 PyO3 绑定构建）
- `maturin`：未安装

### Readiness 结论
- 代码层 infra：已基本齐全。
- 训练运行层依赖：尚未满足，当前不具备真实 `BC/PPO` 训练执行条件。

## 4. 训练进展（截至当前）

- 当前以“先搭 infra”为主，未进入正式训练。
- 暂无可用 BC/PPO checkpoint 产物。
- `results/` 已按要求清空，当前没有保留训练或评估结果文件。

## 5. 下一步（仅建议，未执行）
- 安装训练依赖：`torch`、`gymnasium`、`pytest`、`maturin`。
- 构建并安装 `pylatro`，将 env backend 从 mock 切换为真实引擎。
- 再执行：`phase1 doctor + baseline`，确认 gate 后进入 `phase2`。
